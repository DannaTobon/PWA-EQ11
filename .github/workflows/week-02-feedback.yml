name: Semana 2 — tests y build
on:
  push:
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20.19.6'
          cache: npm

      - name: Instalación reproducible
        run: npm ci --no-audit --no-fund

      - name: Tests de manifest (Vitest)
        run: npm run test:manifest

      - name: Build de producción
        run: npm run build

      - name: Guardar resultado de Vitest
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pwa-eq11-week-02-vitest-report
          if-no-files-found: warn
          path: |
            vitest-report.json
